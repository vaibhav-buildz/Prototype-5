---
description: Apply theme updates from retheme.js
---
# Apply theme updates

Iterate and apply global style variables across `style.css`.

// turbo
1. Execute the retheme script.
```bash
node retheme.js
```
The script will update the `:root` and other selectors in `style.css` based on current project definitions.
